SignIn:
  required: true
  content:
    application/json:
      schema:
        $ref: "schemas.yaml#/SignIn"

SignUp:
  required: true
  content:
    application/json:
      schema:
        $ref: "schemas.yaml#/SignUp"
