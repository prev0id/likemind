package button

type State struct {
	Text string
}

templ Component(state State) {
	<button
		class="w-full text-white bg-red focus:ring-4 focus:outline-none focus:ring-orange font-medium rounded-lg text-sm px-5 py-2.5 text-center"
	>
		{ state.Text }
	</button>
}
