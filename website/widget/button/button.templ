package button

type State struct {
	Text string
}

templ Component(state State) {
	<button
		type="submit"
		class="w-full text-main-white bg-main-red focus:ring-4 focus:outline-none focus:ring-main-orange font-medium rounded-lg text-sm px-5 py-2.5 text-center"
	>
		{ state.Text }
	</button>
}
